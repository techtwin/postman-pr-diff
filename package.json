{
  "name": "postman-pr-diff",
  "version": "1.0.0",
  "private": true,
  "description": "A secure GitHub Action that summarizes semantic Postman collection changes in pull requests.",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "fast-xml-parser": "^5.2.5",
    "fast-xml-parser": "^5.11.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
